<% if flash[:error] %>
  <% flash[:error].each do |error| %>
    <%= error %><br>
  <% end %>
<% end %>


<%= form_for @listing do |f| %>
  <%= %><br>
  <%= f.label :product %>
  <%= f.collection_select :product_id, @products, :id, :name %> <br>

  <%= f.submit %>
  <% end %>
